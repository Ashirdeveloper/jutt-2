CREATE TABLE Students (
    student_id INT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    gender VARCHAR(10),
    grade VARCHAR(5),
    email VARCHAR(100),
    phone VARCHAR(15),
    address VARCHAR(150)
);
